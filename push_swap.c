
#include "push_swap.h"

int	main(int argc, char **argv)
{
	int	start;
	size_t	operations;

	t_stack	*a;
	t_stack *b;



	b = NULL;
	operations = 0;
	while (argv[j])
	{
		if (ft_strcmp(argv[j], "--bench") == 0)

		if (ft_strcmp(argv[j] "--adaptive") == 0)
			operations +=compute_disorder(a);
		else if (ft_strcmp(argv[j], "--simple") == 0)
			operations += simple_sort(start, argc, argv);
		else if (ft_strcmp(argv[j], "--medium") == 0)
			operations +=medium_sort(start, argc, argv);
		else if (ft_strcmp(argv[j], "--complex") == 0)
			operations += complex_sort(start, argc, argv);

		else
			number_stack()
		{
		}
		j++;
	}
}
// sa sb - ss √

// pa pb

// ra rb rr

// rra rrb rrr
